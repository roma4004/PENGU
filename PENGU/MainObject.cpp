#include "MainObject.h"	  

MainObject::MainObject()
//MainObject::MainObject(String imgPath, Color setTransparant, Vector2f(vectPos) )
{
//	Image objImage;                                     // создаем объект Image (изображение)
//	objImage.loadFromFile(imgPath);                    // загружаем в него файл		 
//	objImage.createMaskFromColor(setTransparant);     // загружаем в него файл
//	Texture objTexture;                              // создаем объект Texture (текстура)
//	objTexture.loadFromImage(objImage);             // передаем в него объект Image (изображения)	
//	Sprite objSprite;                              // создаем объект Sprite(спрайт)
//	objSprite.setTexture(objTexture);             // передаём в него объект Texture (текстуры)
//	objSprite.setPosition(vectPos.x, vectPos.y); // задаем начальные координаты появления спрайта
}


MainObject::~MainObject()
{
}
